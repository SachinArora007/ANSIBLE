---
- name: Deploy Node.js app configuration
  hosts: all
  become: yes

  roles:
    - role: deploy_nodejs_app
      vars:
        app_port: 8080
        env_vars:
          NODE_ENV: staging
          DB_HOST: db.staging.example.com
